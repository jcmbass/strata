export const Card = () => {
  return (
    <section>
      {/* Header — drag handle */}

      {/* Terminal body — absolutely positioned rows so Pretext owns the layout */}

      {/* Footer */}
      <div>
        <span>Footer</span>
      </div>
    </section>
  );
}